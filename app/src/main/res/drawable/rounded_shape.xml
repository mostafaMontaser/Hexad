<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">

    <solid android:color="@android:color/white" />

    <stroke
            android:width="@dimen/rounded_stoke"
            android:color="@android:color/background_dark" />

    <padding
            android:bottom="@dimen/rounded_radius"
            android:left="@dimen/rounded_radius"
            android:right="@dimen/rounded_radius"
            android:top="@dimen/rounded_radius" />

    <corners android:radius="@dimen/rounded_radius" />

</shape>